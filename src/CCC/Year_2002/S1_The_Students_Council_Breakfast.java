package CCC.Year_2002;

import java.io.*;

public class S1_The_Students_Council_Breakfast {
    public static void main(String[] args) throws IOException {
        BufferedReader user = new BufferedReader(new InputStreamReader(System.in));

        int pink = Integer.parseInt(user.readLine());
        int green = Integer.parseInt(user.readLine());
        int red = Integer.parseInt(user.readLine());
        int orange = Integer.parseInt(user.readLine());
        int amountToBeRaised = Integer.parseInt(user.readLine());

        int numOfCombinations = 0;
        int minNumOfTickets = Integer.MAX_VALUE;

        int cost = 0;
        int tickets = 0;

        for (int i = 0; i < Math.pow(3, 3); i++) {
            while (true) {

            }
        }
    }
}
